{
	"type": "module",
	"scripts": {
		"build": "tsc transport.ts --target es2016 --module ES2015 --strict --esModuleInterop --skipLibCheck --outDir .",
		"check": "tsc --noEmit",
		"test": "tsc --outDir .test-dist && node .test-dist/transport.test.js"
	},
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
